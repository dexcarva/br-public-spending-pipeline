# Dependências do pipeline (ingestão + transformação).
# Propositalmente enxuto: cada lib aqui tem um motivo claro, sem "porque sim".
#
# requests     -> chamar a API REST do Portal da Transparência.
# duckdb       -> banco analítico embarcado (roda dentro do processo Python,
#                 sem precisar instalar/configurar um servidor de banco).
# dbt-core     -> motor de transformação SQL (staging -> dimensões -> fato).
# dbt-duckdb   -> "adapter" que ensina o dbt a falar com o DuckDB em vez de
#                 um banco tradicional como Postgres/Snowflake/BigQuery.
requests==2.32.3
duckdb==1.1.3
dbt-core==1.8.9
dbt-duckdb==1.8.4
